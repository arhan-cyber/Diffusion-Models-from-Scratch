training:
  epochs: 25
  batch_size: 128
  learning_rate: 0.0001
  noise_steps: 1000
  sample_every: 5
  checkpoint_every: 5
  use_wandb: false
  wandb_project: "diffusion-letters"
model:
  base_channels: 64
  depth: 3
  emb_dim: 256
